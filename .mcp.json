{
  "mcpServers": {
    "loomcycle": {
      "command": "${user_config.bin_path}",
      "args": ["mcp", "--config", "${user_config.config_path}"],
      "env": {
        "LOOMCYCLE_AUTH_TOKEN": "${user_config.auth_token}"
      }
    }
  }
}
