{
  "mcpServers": {
    "loomcycle": {
      "command": "${user_config.bin_path}",
      "args": ["mcp", "--upstream", "${user_config.base_url}"],
      "env": {
        "LOOMCYCLE_MCP_UPSTREAM_TOKEN": "${user_config.auth_token}"
      }
    }
  }
}
